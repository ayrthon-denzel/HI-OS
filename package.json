{
  "name": "hi-os",
  "version": "0.4.0",
  "private": true,
  "description": "HI MARKETING Operating System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check server/ai.js && node --check server/job-worker.js && node --check js/auth.js && node --check js/app.js && node --check js/job-service.js && node --check js/live-api.js",
    "security:audit": "npm audit --omit=dev --audit-level=high"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "mammoth": "^1.10.0",
    "multer": "^2.0.2",
    "otplib": "^12.0.1",
    "pdf-parse": "^1.1.1",
    "pg": "^8.16.3"
  }
}
